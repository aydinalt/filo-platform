import {describe,it} from "node:test";import assert from "node:assert/strict";import {createHmac} from "node:crypto";import {verifyProviderSignature} from "../src/lib/provider-signature.js";
describe("provider webhook signatures",()=>{it("accepts an authentic recent payload and rejects tampering",()=>{const now=Date.now(),timestamp=String(Math.floor(now/1000)),payload=JSON.stringify({eventId:"evt-1"}),secret="test-webhook-secret-at-least-16",signature=createHmac("sha256",secret).update(`${timestamp}.${payload}`).digest("hex");assert.equal(verifyProviderSignature(payload,timestamp,signature,secret,now),true);assert.equal(verifyProviderSignature(payload+"x",timestamp,signature,secret,now),false);assert.equal(verifyProviderSignature(payload,String(Number(timestamp)-301),signature,secret,now),false);});});
