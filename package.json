{
  "name": "filo-platform-v1",
  "private": true,
  "version": "0.44.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @filo/api & npm run dev --workspace @filo/web",
    "build": "npm run build --workspace @filo/contracts && npm run build --workspace @filo/database && npm run build --workspace @filo/api && npm run build --workspace @filo/web",
    "typecheck": "npm run build --workspace @filo/contracts && npm run build --workspace @filo/database && npm run typecheck --workspace @filo/api && npm run typecheck --workspace @filo/web",
    "test": "npm run test --workspaces --if-present",
    "db:migrate": "npm run db:migrate --workspace @filo/database",
    "db:seed": "npm run db:seed --workspace @filo/database"
  },
  "engines": {
    "node": ">=22"
  }
}
