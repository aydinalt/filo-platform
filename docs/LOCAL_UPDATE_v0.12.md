# Local Update v0.12

Adds tenant-isolated driver safety events, operational review/resolution, audit records and fleet safety summaries.

Run `npm install`, `npm run db:migrate`, `npm run typecheck`, `npm test`, then `npm run build`.
